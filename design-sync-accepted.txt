# Различия между макетом Claude Design (проект «Classical», apps/marginalia/app.css)
# и собранными стилями приложения, принятые осознанно.
#
# Сверка:
#   python3 ~/.claude/skills/skill-design-sync/scripts/css_diff.py \
#     <выгруженный app.css> dist/assets/index-*.css --accepted design-sync-accepted.txt
#
# Запись, переставшая соответствовать реальному расхождению, роняет сверку как
# устаревшая — список не может пережить различие, которое он описывает.
#
# 16.08.2026 макет пересобран из текущей сборки, и список сократился с сорока
# пяти записей до двенадцати. Прежние сорок пять описывали не решения, а возраст
# макета: он был снят задолго до хранилища, диалогов вставки и переименования,
# узких раскладок и счётчиков в шапке, и каждая новая функция приложения
# дописывала туда строку. Такой список растёт монотонно и однажды перестаёт
# показывать настоящее расхождение — оно тонет среди принятых.

# Шрифты лежат рядом с app.css, а не в каталоге assets собранного приложения:
# карточка предпросмотра раздаётся из своей папки проекта Design, и путь от
# корня сайта в ней не разрешается. Отличается только значение src, начертания
# и диапазоны символов совпадают. Тринадцатый @font-face — Material Symbols,
# он встроен как data: и потому сходится.
differs: @font-face[0] -> src
differs: @font-face[1] -> src
differs: @font-face[2] -> src
differs: @font-face[3] -> src
differs: @font-face[4] -> src
differs: @font-face[5] -> src
differs: @font-face[6] -> src
differs: @font-face[7] -> src
differs: @font-face[8] -> src
differs: @font-face[9] -> src
differs: @font-face[10] -> src
differs: @font-face[11] -> src
